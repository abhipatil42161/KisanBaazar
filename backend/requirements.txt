# KisanBaazar backend — production requirements (Render / Python 3.11).
# Only packages directly imported by the runtime code. Dev/test tools
# (pytest, black, bandit, flake8, mypy, etc.) are NOT included here.

# --- Web framework ---
fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2
python-multipart==0.0.32

# --- Data layer ---
motor==3.3.1
pymongo==4.6.3

# --- Validation / models ---
pydantic==2.13.4
email-validator==2.3.0

# --- Config / env ---
python-dotenv==1.2.2

# --- Auth ---
PyJWT==2.13.0
bcrypt==4.1.3

# --- HTTP ---
httpx==0.28.1

# --- Integrations ---
anthropic==0.115.0
cloudinary==1.44.3
razorpay==2.0.1

# --- PDF invoice generation ---
reportlab==5.0.0

# --- Email (Resend for OTP + transactional email) ---
resend==2.32.2
